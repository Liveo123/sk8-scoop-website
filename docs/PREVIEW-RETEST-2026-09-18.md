# Preview secret retest — 18 September 2026

Owner confirmed the Cloudflare non-production deploy command was changed so `RESEND_API_KEY` is supplied to preview Worker versions via a secrets file.

This commit exists to trigger a fresh Cloudflare non-production build for `sk8-business-growth-engine-v2` before the end-to-end advertiser enquiry retest.

After verification, this note can remain as an audit trail or be removed before merge.
